.page-header
  h1 New exercise_def

- form_for(exercise_def, {action: path_to.exercise_defs(), method: 'POST', id: "exercise_def_form"}, function (form) {
  != partial('exercise_defs/form.jade', {locals: {form: form, exercise_def: exercise_def}})
  .actions
    != form.submit('Create exercise_def', {class: 'btn'})
    span= ' or '
    != link_to('Cancel', path_to.exercise_defs(), {class: 'btn'})
- });
